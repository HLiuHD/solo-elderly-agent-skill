[project]
name = "news-extractor"
version = "0.1.0"
description = "pre_llm script for news content extraction"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "beautifulsoup4>=4.12",
]
